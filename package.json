{
  "name": "verifa-ai-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "start:backend": "npm run start --prefix backend",
    "build": "npm run build --prefix frontend"
  }
}
